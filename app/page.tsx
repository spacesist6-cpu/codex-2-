"use client";

import { useEffect, useMemo, useState } from "react";
import { createClient, type SupabaseClient } from "@supabase/supabase-js";

type Result = { id: string; student: string; grade: string; subject: string; created_at: string; content: string };
type AgentState = "idle" | "collect" | "write" | "review" | "done" | "error";

const ui = {
  brand: "\uC138\uD2B9 \uBA54\uC774\uCEE4", newDraft: "\uC0C8 \uCD08\uC548 \uB9CC\uB4E4\uAE30", history: "\uC800\uC7A5 \uB0B4\uC5ED", settings: "\uAC1C\uC778 \uC124\uC815", workspace: "\uC791\uC5C5 \uACF5\uAC04", teacher: "\uB2F4\uB2F9 \uAD50\uC0AC", information: "\uC815\uBCF4 \uAD50\uACFC", title: "\uC138\uD2B9 \uBB38\uAD6C \uCD08\uC548", grade: "\uD559\uB144", gradeUnit: "\uD559\uB144", subject: "\uACFC\uBAA9", student: "\uD559\uC0DD \uC2DD\uBCC4\uAC12", activity: "\uD65C\uB3D9 \uD0A4\uC6CC\uB4DC / \uAD00\uCC30 \uB0B4\uC6A9", input: "\uD559\uC0DD \uD65C\uB3D9 \uC785\uB825", output: "\uACFC\uBAA9\uBCC4 \uC138\uD2B9 \uCD08\uC548", make: "\uCD08\uC548 \uC0DD\uC131\uD558\uAE30", download: "\uD14D\uC2A4\uD2B8 \uB2E4\uC6B4\uB85C\uB4DC", save: "\uACB0\uACFC \uC800\uC7A5", collect: "\uC218\uC9D1", write: "\uC791\uC131", review: "\uAC80\uD1A0", complete: "\uAC80\uD1A0 \uC644\uB8CC", empty: "\uC544\uC9C1 \uC0DD\uC131\uB41C \uCD08\uC548\uC774 \uC5C6\uC2B5\uB2C8\uB2E4.", workflow: "\uD65C\uB3D9 \uD0A4\uC6CC\uB4DC\uB97C \uC785\uB825\uD558\uBA74 \uC218\uC9D1, \uC791\uC131, \uAC80\uD1A0 \uC5D0\uC774\uC804\uD2B8\uAC00 \uC21C\uC11C\uB300\uB85C \uC791\uC5C5\uD569\uB2C8\uB2E4.", hint: "\uD559\uC2B5 \uACFC\uC81C\uBCC4 \uD0A4\uC6CC\uB4DC\uB098 \uAD00\uCC30 \uB0B4\uC6A9\uC744 \uC904\uBC14\uAFC8\uC73C\uB85C \uAD6C\uBD84\uD574 \uC785\uB825\uD558\uC138\uC694.", error: "\uC0DD\uC131 \uC624\uB958", wait: "\uC5D0\uC774\uC804\uD2B8\uAC00 \uC21C\uC11C\uB300\uB85C \uC791\uC5C5\uD569\uB2C8\uB2E4.", saved: "\uC800\uC7A5\uB41C \uCD08\uC548", total: "\uCD1D", countUnit: "\uAC74", activityWord: "\uD65C\uB3D9", inputKicker: "\uC785\uB825", outputKicker: "\uACB0\uACFC", sectionWork: "3\uB2E8\uACC4 \uC791\uC5C5", model: "\uC120\uD638 \uBAA8\uB378",
};

const supabase: SupabaseClient | null = process.env.NEXT_PUBLIC_SUPABASE_URL && process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY ? createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY) : null;

export default function Home() {
  const [active, setActive] = useState("new");
  const [grade, setGrade] = useState("2");
  const [subject, setSubject] = useState("\uC815\uBCF4");
  const [student, setStudent] = useState("2026-\uC815\uBCF4-017");
  const [keywords, setKeywords] = useState("\uD30C\uC774\uC36C\uC73C\uB85C CSV \uB370\uC774\uD130\uB97C \uBD88\uB7EC\uC640 \uACB0\uCE21\uAC12\uC744 \uCC98\uB9AC\uD568\n\uBC18\uBCF5\uBB38\uACFC \uB9AC\uC2A4\uD2B8\uB97C \uD65C\uC6A9\uD574 \uD3C9\uADE0\uC744 \uACC4\uC0B0\uD568\n\uC624\uB958\uAC00 \uB09C \uCF54\uB4DC\uB97C \uC6D0\uC778\uBCC4\uB85C \uC218\uC815\uD558\uACE0 \uC2E4\uD589 \uACB0\uACFC\uB97C \uAE30\uB85D\uD568");
  const [model, setModel] = useState("Gemini 3.5 Flash-Lite");
  const [apiKey, setApiKey] = useState("");
  const [agent, setAgent] = useState<AgentState>("idle");
  const [output, setOutput] = useState<string[]>([]);
  const [results, setResults] = useState<Result[]>([]);
  const [saved, setSaved] = useState(false);
  const [error, setError] = useState("");
  const [selected, setSelected] = useState<Result | null>(null);
  const activities = useMemo(() => keywords.split("\n").map((item) => item.trim()).filter(Boolean), [keywords]);
  useEffect(() => { void loadResults(); }, []);
  useEffect(() => {
    const root = document.querySelector(".app-shell");
    root?.querySelectorAll("*").forEach((element) => {
      if (element.childNodes.length === 1 && element.firstChild?.nodeType === Node.TEXT_NODE) {
        element.textContent = element.textContent?.replace(/\\u([0-9a-f]{4})/gi, (_, code: string) => String.fromCharCode(parseInt(code, 16))) || "";
      }
    });
  }, [active, agent, output.length, results.length, selected]);
  async function loadResults() { if (supabase) { const { data } = await supabase.from("seteuk_results").select("*").order("created_at", { ascending: false }); if (data) { setResults(data as Result[]); return; } } setResults([]); }
  async function generate() { if (!keywords.trim()) return; setError(""); setSaved(false); setAgent("collect"); try { const response = await fetch("/api/generate", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ apiKey, model, grade: `${grade}${ui.gradeUnit}`, subject, student, activities }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "API request failed"); setAgent("write"); await new Promise((resolve) => setTimeout(resolve, 350)); setAgent("review"); await new Promise((resolve) => setTimeout(resolve, 350)); setOutput(data.results); setAgent("done"); } catch (caught) { setError(caught instanceof Error ? caught.message : "API request failed"); setAgent("error"); } }
  async function saveResult() { const row: Result = { id: crypto.randomUUID(), student, grade: `${grade}${ui.gradeUnit}`, subject, created_at: new Date().toISOString(), content: output.join("\n\n") }; if (supabase) { const { error: dbError } = await supabase.from("seteuk_results").insert(row); if (dbError) { setError(dbError.message); return; } } setResults((previous) => [row, ...previous]); setSaved(true); }
  function download() { const blob = new Blob([output.join("\n\n")], { type: "text/plain;charset=utf-8" }); const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = `${student}-${subject}-seteuk.txt`; link.click(); URL.revokeObjectURL(url); }

  const sidebarProgress = <div className="sidebar-progress"><div className="sidebar-progress-head"><span>{ui.sectionWork}</span><small>{agent === "done" ? ui.complete : agent === "idle" ? ui.wait : agent === "error" ? ui.error : ui.wait}</small></div><div className={`sidebar-agent ${agent === "collect" ? "current" : agent === "write" || agent === "review" || agent === "done" ? "complete" : ""}`}><span className="agent-dot">01</span><div><b>{ui.collect}</b><small>{agent === "collect" ? "..." : agent === "idle" ? "" : "OK"}</small></div></div><div className={`sidebar-agent ${agent === "write" ? "current" : agent === "review" || agent === "done" ? "complete" : ""}`}><span className="agent-dot">02</span><div><b>{ui.write}</b><small>{agent === "write" ? "..." : agent === "review" || agent === "done" ? "OK" : ""}</small></div></div><div className={`sidebar-agent ${agent === "review" ? "current" : agent === "done" ? "complete" : ""}`}><span className="agent-dot">03</span><div><b>{ui.review}</b><small>{agent === "review" ? "..." : agent === "done" ? "OK" : ""}</small></div></div></div>;
  const navigation = <aside className="sidebar"><div className="brand"><span className="brand-mark">S</span><span>{ui.brand}</span></div><div className="workspace-label">{ui.workspace}</div><button className={active === "new" ? "nav-item active" : "nav-item"} onClick={() => setActive("new")}><span>+</span>{ui.newDraft}</button><button className={active === "history" ? "nav-item active" : "nav-item"} onClick={() => setActive("history")}><span>H</span>{ui.history}<em>{results.length}</em></button>{sidebarProgress}<div className="sidebar-bottom"><button className={active === "settings" ? "nav-item active" : "nav-item"} onClick={() => setActive("settings")}><span>S</span>{ui.settings}</button><div className="user-card"><div className="avatar">T</div><div><b>{ui.teacher}</b><small>{ui.information}</small></div><span className="dot" /></div></div></aside>;
  const header = <header className="topbar"><div><span className="eyebrow">{ui.information}</span><h1>{active === "new" ? ui.title : active === "history" ? ui.history : ui.settings}</h1></div><div className="connection"><span className="status-dot" />{supabase ? "Supabase \uC5F0\uACB0\uB428" : "\uB370\uBAA8 \uBAA8\uB4DC"}</div></header>;
  const settings = <div className="settings-panel panel"><span className="section-kicker">{ui.settings}</span><h2>{"\uC0DD\uC131 \uD658\uACBD \uC124\uC815"}</h2><p className="muted">{"Gemini API Key\uC640 \uC120\uD638 \uBAA8\uB378\uC744 \uC124\uC815\uD569\uB2C8\uB2E4. \uD0A4\uB294 \uC774 \uC138\uC158\uC5D0\uC11C\uB9CC \uC0AC\uC6A9\uD569\uB2C8\uB2E4."}</p><label>Gemini API Key<input value={apiKey} onChange={(event) => setApiKey(event.target.value)} type="password" placeholder="API Key" /></label><label>{ui.model}<select value={model} onChange={(event) => setModel(event.target.value)}><option>Gemini 3.5 Flash-Lite</option><option>Gemini 2.5 Flash</option><option>Gemini 2.5 Pro</option></select></label></div>;
  const history = <div className="history-wrap"><div className="history-summary"><div><span className="section-kicker">{ui.history}</span><h2>{ui.history}</h2></div><span className="count-pill">{ui.total} {results.length}{ui.countUnit}</span></div>{results.map((item) => <button className="history-row" key={item.id} onClick={() => setSelected(item)}><div className="history-icon">TXT</div><div className="history-main"><b>{item.student} / {item.subject}</b><p>{item.content.slice(0, 90)}...</p></div><div className="history-meta"><span>{item.grade}</span><small>{new Intl.DateTimeFormat("ko-KR", { dateStyle: "medium" }).format(new Date(item.created_at))}</small></div><span className="chevron">&gt;</span></button>)}{selected && <div className="detail panel"><div className="detail-head"><div><span className="section-kicker">{ui.saved}</span><h2>{selected.student} / {selected.subject}</h2></div><button className="icon-button" onClick={() => setSelected(null)}>X</button></div><p>{selected.content}</p></div>}</div>;
  const newDraft = <><div className="intro"><div><span className="section-kicker">{ui.sectionWork}</span><p>{ui.workflow}</p></div><div className="agent-steps"><span className={agent === "collect" ? "on" : agent !== "idle" && agent !== "error" ? "complete" : ""}>01 {ui.collect}</span><i>&gt;</i><span className={agent === "write" ? "on" : agent === "review" || agent === "done" ? "complete" : ""}>02 {ui.write}</span><i>&gt;</i><span className={agent === "review" ? "on" : agent === "done" ? "complete" : ""}>03 {ui.review}</span></div></div><div className="grid"><div className="input-column panel"><span className="section-kicker">{ui.input}</span><h2>{ui.input}</h2><p className="muted">{ui.hint}</p><div className="field-row"><label>{ui.grade}<select value={grade} onChange={(event) => setGrade(event.target.value)}><option value="1">1{ui.gradeUnit}</option><option value="2">2{ui.gradeUnit}</option><option value="3">3{ui.gradeUnit}</option></select></label><label>{ui.subject}<input value={subject} onChange={(event) => setSubject(event.target.value)} /></label></div><label>{ui.student}<input value={student} onChange={(event) => setStudent(event.target.value)} /></label><label>{ui.activity}<textarea value={keywords} onChange={(event) => setKeywords(event.target.value)} rows={8} /></label><div className="input-footer"><span>{activities.length}{ui.activityWord} \uC778\uC2DD</span><button className="primary" onClick={generate} disabled={agent !== "idle" && agent !== "done" && agent !== "error"}>{agent !== "idle" && agent !== "done" && agent !== "error" ? "\uC0DD\uC131 \uC911..." : ui.make}</button></div>{error && <p className="error-message">{error}</p>}</div><div className="output-column"><div className="output-header"><div><span className="section-kicker">{ui.output}</span><h2>{ui.output}</h2></div>{agent === "done" && <span className="ready-pill">OK {ui.complete}</span>}</div>{agent === "idle" && <div className="empty-state"><div className="empty-mark">+</div><h3>{ui.empty}</h3><p>{ui.hint}</p></div>}{agent !== "idle" && agent !== "done" && agent !== "error" && <div className="loading-state"><div className="loader" /><h3>{agent === "collect" ? "\uD65C\uB3D9 \uB0B4\uC6A9\uC744 \uC815\uB9AC\uD558\uACE0 \uC788\uC5B4\uC694" : agent === "write" ? "\uC138\uD2B9 \uBB38\uAD6C\uB97C \uC0DD\uC131\uD558\uACE0 \uC788\uC5B4\uC694" : "\uD45C\uD604 \uADDC\uC815\uC744 \uAC80\uD1A0\uD558\uACE0 \uC788\uC5B4\uC694"}</h3><p>{ui.wait}</p></div>}{agent === "error" && <div className="empty-state"><div className="empty-mark">!</div><h3>{ui.error}</h3><p>{ui.hint}</p></div>}{agent === "done" && <div className="results-list">{output.map((item, index) => <article className="result-card" key={`${item}-${index}`}><div className="result-top"><span className="activity-num">{ui.activityWord} {String(index + 1).padStart(2, "0")}</span><span className="reviewed">OK \uAC80\uD1A0\uB428</span></div><p>{item}</p></article>)}<div className="result-actions"><button className="secondary" onClick={download}>{ui.download}</button><button className="primary" onClick={saveResult}>{saved ? "\uC800\uC7A5 \uC644\uB8CC" : ui.save}</button></div></div>}</div></div></>;
  return <main className="app-shell">{navigation}<section className="content">{header}{active === "settings" ? settings : active === "history" ? history : newDraft}</section></main>;
}
